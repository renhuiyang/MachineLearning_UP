# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                 controllers.Application.index
GET     /login                            controllers.Application.login
GET     /query/:id                        controllers.Application.query(id:String)
GET     /result                           controllers.Application.result
GET     /download/:result                 controllers.Application.download(result:String)
GET     /readExitsModel                   controllers.Application.readExitsModel

POST    /create                           controllers.Application.create
POST    /predict                          controllers.Application.predict

GET     /createModel                      controllers.Application.createModel
GET     /uploadTarget/:model              controllers.Application.uploadTarget(model:String)

#static view

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                     controllers.Assets.versioned(path="/public", file: Asset)
